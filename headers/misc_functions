

#if not defined(_misc_functions_)
    #define _misc_functions_

    
    #include <cstdio>
    #include <string>
    #include <unordered_map>
    #include <vector>

    namespace misc_functions {

        typedef enum {
            DIRECTORY,
            FILE
        } content_type;


        std::string get_current_time();

        int get_terminal_width();

        int get_terminal_height();

        bool has_keyboard_input(const struct timeval timeout = {0, 200});

        std::unordered_map<content_type, std::vector<std::string> > get_directory_content(const std::string& directory, const bool complete_path = false);

        std::string get_input(const std::string prompt = "", std::FILE* the_file = stdin, const int k_count = 1);

    }

#endif